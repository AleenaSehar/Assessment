pipeline {
    agent any

    stages {
        stage('Clone Repo') {
            steps {
                // Clones your GitHub repo
                git branch: 'main', url: 'https://github.com/AleenaSehar/Assessment.git'
            }
        }

        stage('Build') {
            steps {
                echo 'Building the frontend...'
                // For example, if React:
                sh 'npm install'
                sh 'npm run build'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
                // Add test commands if needed
            }
        }
    }
}
